# Hookvox 上線前檢查清單

綠界審核通過後、正式對外前，建議依此清單檢查一輪。

---

## 一、安全 (Security)

| 項目 | 狀態 | 說明 |
|------|------|------|
| 環境變數 / 密鑰 | ✅ | 不提交 `.env*`；DB / Supabase Service Role 等僅在 Vercel / 本機，且已旋轉過 |
| RLS 與政策 | ✅ | `users`、`subscriptions`、`orders`、`usage_logs`、`viral_database` 等已開 RLS + 政策 |
| 上傳儲存 | ✅ | `analyze-uploads` Private，僅允許登入者寫入自己路徑、副檔名白名單 |
| 安全標頭 | ✅ | CSP、HSTS、X-Frame-Options、nosniff 等已在 `next.config.js` |
| 登入/註冊導向 | ✅ | `redirect` 僅允許同源路徑，無 open redirect |
| 解析連結防濫用 | ✅ | `/api/resolve-url` 需登入、限流、僅允許 YouTube/IG/TikTok |
| ECPay 回調 | ✅ | CheckMacValue 驗證、回調事件寫入 `payment_webhook_events` 稽核 |
| 定期檢查 | 建議 | 每季或重大更新後跑 `npm audit`，必要時修補依賴 |

**已加好**：Sentry 已接好（選填 `NEXT_PUBLIC_SENTRY_DSN` 即啟用）。詳見 `docs/照做步驟-檢查與補強.md`。

---

## 二、SEO

| 項目 | 狀態 | 說明 |
|------|------|------|
| 全站 metadata | ✅ | `layout.tsx` 已有 title、description、keywords |
| Open Graph / Twitter | ✅ | 已加 `openGraph`、`twitter`，分享預覽正常 |
| robots.txt | ✅ | `public/robots.txt`，Allow /，Sitemap 指向 `/sitemap.xml` |
| sitemap.xml | ✅ | `src/app/sitemap.ts` 動態產生，含首頁、方案、成功案例、指南、隱私/條款/退款 |

**可選**：重要頁面可再加 `metadataBase`（若 Next 建議）或各頁專屬 description。

---

## 三、金流與營運（綠界審核通過後）

| 項目 | 說明 |
|------|------|
| 付款完成回傳網址 | 設為 `https://hookvox-1yib.vercel.app/api/ecpay/notify` |
| 測試一筆真實付款 | 確認訂單狀態變 PAID、訂閱升級、`payment_webhook_events` 有 SUCCESS |
| 販售網址 / 商店資料 | 已改為正式網址與商店圖示 |

---

## 四、合規與法務

| 項目 | 說明 |
|------|------|
| 隱私權政策 | ✅ 有 `/privacy` |
| 服務條款 | ✅ 有 `/terms` |
| 退款政策 | ✅ 有 `/refund` |
| 若收個資 | 確認隱私權政策涵蓋蒐集目的、保存期限、當事人權利（查詢/刪除等） |

---

## 五、監控與備援

| 項目 | 說明 |
|------|------|
| AI 用量與預算 | 已設 OpenAI / Anthropic 用量與預算告警 |
| Vercel | 專案用正式網址，env 已設好 |
| Supabase | 每日備份為 Pro 方案功能；免費方案可改為自行定期匯出重要表或升級 Pro |
| 日誌 | Sentry 已接好（選填 DSN 即啟用），ECPay 回調已有 DB 稽核 |

---

## 六、其他建議

- **Analytics**：已加 Vercel Analytics，部署後在 Vercel 後台可看流量；若隱私權政策尚未提及，可補一句「使用 Vercel Analytics 蒐集匿名流量數據」。
- **效能**：上線後用 Lighthouse / PageSpeed 跑首頁與主要流程，必要時優化 LCP、CLS。
- **網域**：若之後改用自有網域，記得改 `NEXT_PUBLIC_APP_URL`、綠界回傳網址、`robots.txt` 與 sitemap 的網址。

---

綠界審核通過後，把「付款完成回傳網址」設好並完成一筆測試付款，即可視為金流就緒；其餘項目可依清單逐項打勾即可。
